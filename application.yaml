spring:
  jpa:
    properties:
      hibernate:
        ddl-auto: validate
        format_sql: true
        generate_statistics: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka